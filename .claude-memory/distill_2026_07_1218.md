---
name: distill_2026_07_1218
description: 2026-07-24 月次蒸留 MADB 1.2.18 取込の結果と積み残し
metadata: 
  node_type: memory
  type: project
  originSessionId: dff8a305-89f9-41d1-baa4-d9b9d0478784
  modified: 2026-07-24T23:24:40.729Z
---

2026-07-24 に MADB **1.2.18**(2026-07-17リリース)を月次蒸留で取込。前回=1.2.17。

## 中核=完了・検証済み(commit 1e107d9a4 push済)
- 種2 増分merge(INSERT-only): series 167,047→**167,339**(+292) / vol 384,209→**385,333**(+1,124) / ed +307。削除0・上書き0。純増1,124は月間実績1,000-1,300と整合。
- backup: `.cache/db-v2.sqlite.bak-distill-2026-07-24` + manifest `merge-manifest-1.2.18.json`。
- promote: 本番 manga.v2 **66,404頁**再生成(preorder合流1478)。索引フル再生成(67,882作・**skip 0**) + calendar/corner/sansedai(737)/featured(54)更新。
- 検証: tsc green / vitest **241/241** green。
- サニティ監査: AUTO_FIXED **1,685**(baseline~1,677=急増なし=新型signal無し) / MISSING_HALF 55 / GAP_OTHER 6,281 / title==author 6 / solo-truncated 173 / foreign冪等(0新規) / junk garbled27・no_author1,267(既存backlog)。

## この蒸留で直した2つのスクリプト間バグ(=型)
- **trailing**: `series-merge-auto.json` schema が `merge_sids`→`merge_keys`(=series_key)に変わったのに `_audit-trailing-gaps.py` が旧keyで KeyError→intake abort。merge_keys対応で修正。
- **foreigndrop**: `_audit-foreign-editions.py --apply` が外国版dropを**2スペース**インデントで追記し non-manga-drop.yml をパース不能に→intake abort。0スペースへ是正+既存汚染59件修復。[[non_manga_drop_cleanup]]

## 積み残し(AI生成heavy=別セッション/idle/週次で)
- ~~synopsis和訳 backlog 9,421~~ → ★**2026-07-25 完走(commit 3c728944a)**。実測で「~94 batch」は誤りだった:
  未訳9,421の内訳 = AniList desc **null 7,503 / 空 1,398 / desc有 520**。さらに desc有520のうち
  **215件は本文ゼロ**(`Note: Includes one extra chapter.` 等の注記だけ)= あらすじ不能。
  → **採用305件のみ純粋追加**(seed 39,286→**39,591**、overwrote 0)。抽出器 `_syn-todo-batches.py` /
  合流器 `_syn-merge-out.py` が Note:/Source: を剥いだ「本体」で判定して同じ穴を塞ぐ。
  ★残る ~9,100 は **AniListに翻訳元が無い** = AI和訳では永久に埋まらない。埋めるなら楽天あらすじ等の
  別ソース([[catch_synopsis_enrich_pending]])。1バッチ100件の実測コスト = 約44,000 tokens。[[synopsis_ja_seed]]
- 種3 metadata fill(新292 series)/ AniList鮮度再フェッチ / catch・synopsis・genre enrich(新刊)= [[enrich_7k_resume_state]] や skill enrich-catch-synopsis の領域。
- **本番R2公開は「週次蒸留して」**が必要(この蒸留はディスク上 manga.v2 再生成まで。R2未反映)。

## 手順メモ(次回再利用)
- release marker: `.cache/madb-last-release.txt`=1.2.18 更新済。
- GitHub全件dump手動DL: `curl -sSL -o … https://github.com/mediaarts-db/dataset/releases/download/<tag>/metadata101_json.zip`→展開して `.cache/madb/metadata101.json` に置く。
- temp build: db-v2をcopy→`MADB_DB=…temp MADB_SERIES_V2=…python _populate-v2.py`→`_distill-incremental-merge.py <temp> --apply`。定数(AT/tag/MANIFEST)をリリースに合わせ更新要。
- 差分レポート`_monthly-diff-report.py`は生レコード級でM-id churnにより過大に出る(11,732と出たが真の純増は1,124)。merge dry-runが正。
