---
name: filter_ui_todo_2026_09_05
description: 【着手待ち】検索フィルターUI改善5点(見出しが黒で見えない/件数計算のソート撤去/適用中チップの穴/0件と読込中の区別/並べ替え+アコーディオン)
metadata: 
  node_type: memory
  type: project
  originSessionId: 2a24190d-1d81-4e62-9795-950b199a910c
  modified: 2026-09-05T02:36:46.216Z
---

2026-09-05 ユーザ相談。**`/clear` を挟んで続きをやる約束**。ユーザが「フィルターの続き」等と言ったらこれ。

## 大前提(ユーザ裁定 2026-09-05)

- ★**機能の追加・削除・変更はしない**。UI/操作性だけ直す。
- 外部AIが勧めてきた「**最近使った検索条件**」は localStorage を足す＝新機能なので**やらない**(制約違反)。
- 同じく外部AIの「選択中条件をチップ表示」「結果件数を常に表示」は**既に実装済み**(下記2番の穴を除く)。現物を見ない一般論だった。

## 対象ファイル

- `components/FilterPanel.tsx` (411行) = パネル本体。ホーム(モバイル全画面/PCサイドバー)と `/list`(右ドロワー)で**共用**
- `app/HomeClient.tsx` = モバイル全画面モーダル(~L560-616)、PC側 FilterPanel(~L499)、`結果を見る（N）` L608
- `components/ListClient.tsx` = `/list` の右ドロワー(86%幅)。★こちらは `matchedSlugs` を渡していない(が `/list` は state.query を消す設計なので現状は無害)
- `lib/filters.ts` = `applyFilters` / `sortItems` / `effectiveSort`

## やること5点(この順)

**0. 見出しが黒で見えない**(2026-09-05 ユーザ報告・スクショあり)
- `FilterPanel.tsx:402` の `Section` 見出しが `text-black/60` と**ベタ書き**でテーマ変数を見ていない → ダークで黒背景に黒文字。「種類/連載状態/並び順/創刊/分野/ジャンル…」が全部沈む
- `HomeClient.tsx:579` モーダル背景が `color-mix(in srgb, var(--color-surface) 28%, transparent)` = **72%透過**でコントラストがさらに落ちる → 不透明側へ
- 件数バッジ `FilterPanel.tsx:110` の `Cnt` が `text-ink/40`(0件は `/20`)で薄い → 濃く

**1. 件数計算からソートを外す**(効き目最大・見た目不変)
- `counts` useMemo (`FilterPanel.tsx:35-80`) が state 変更のたび `applyFilters` を**6パス**走らせる
- `applyFilters` は末尾で必ず `sortItems(filtered, effectiveSort(state))` (`filters.ts:176`)、`default` は `popularity` に化ける
- ⇒ **件数を数えるだけなのに 69,236件の配列コピー＋ソートを1タップ6回**。ソート無し経路(option か内部関数)を足して `rowsFor` から使う。~20行

**2. 「適用中の絞り込み」の穴を塞ぐ**
- `FilterPanel.tsx:132-140` の `active[]` に入るのは アニメ化/受賞/連載状態/分野/ジャンル/創刊/著者 **だけ**
- ★**要素・出版社・連載誌・画集・検索語・並び順が入らない** → 出版社で0件にしても画面に痕跡が残らず解除もできない
- 追加＋そのブロックを sticky に。リセットは最下部(`:384`)なので0件時に遠い

**3. 0件と読み込み中を区別**
- 全facet 0 の廃墟になる原因は3つあるのに UI が区別していない: ①索引未到着(22.1MB/69,236件を idle 遅延ロード、届く前は `manga=[]`) ②検索0ヒット(`matchedSlugs` 空集合) ③ANDの絞りすぎ
- 見分けの痕跡: 出版社/連載誌が消えるのは `facetList` の `n>0` 落とし(`:88`)、著者ア〜ワだけ残るのは五十音行が静的配列、画集だけ件数が出るのは `data.artBooks.length` がSSR定数で絞り込み非連動(`:306`)
- ★プレビューで全0のスクショ / 本番 mangal-db.com/browse では 69,242件・完結61,804 と正常表示 = ①か②で確定

**4. セクション並べ替え＋アコーディオン**
- 現順(`:161,180,196,210,274,289,312,327,356,371`)= 種類→連載状態→**並び順**→創刊→分野→ジャンル→要素→出版社→連載誌→著者。絞り込みでない「並び順」が3番目、創刊ドリルダウンがジャンルより上 → ジャンルに着くまで5セクション分スクロール
- ★**折りたたむなら内部スクロールを必ず廃止**: 要素 `max-h-60`(:313)、出版社 `max-h-48`(:329)、連載誌 `max-h-48`(:357) が全画面モーダルのスクロールと競合し「親が動くか子が動くか指で分からない」= 縦の長さより悪い。アコーディオン展開＝全高表示にする
- ★「＋条件を追加」で**全部閉じるのは反対**: 連載誌・創刊年月で絞れることは開いて初めて気づく類=発見性が死ぬ。上位4つは開いたまま、下の重い4つだけ開閉

## 反映ルート

コードのみの変更 → **「機能蒸留して」**(skill `feature-distill`、共有チャンクだけPUT・漫画66k頁と索引は不変)。先に preview で目視。
